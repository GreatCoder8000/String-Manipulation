string = "A"

print(string)

if len(string) == 1:# check for one chracter
	print("the string is one character long")
else:
	print("the string is is not one character long")

string = "A":

if len(string) > 1:# check for more than one character
	print("the string is over a single character")
else:
	print("the string is 1 or 0 characters")

if len(string) > 1:# check for more than one character
	print("the string is over a single character")
elif len(string) == 1:# check for one chracter
	print("the string is a single character")
else:
	print("the string is 1 or 0 characters")
